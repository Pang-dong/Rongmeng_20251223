<Window x:Class="Rongmeng_20251223.ScanCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="扫描 SN 码" Width="360" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        Loaded="Window_Loaded">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请扫描或输入 SN 码：" FontWeight="Bold" FontSize="13"
                   Foreground="#333333" Margin="0,0,0,8"/>

        <TextBox x:Name="TBox_SN" Grid.Row="1" Height="32"
                 VerticalContentAlignment="Center" Padding="6,0"
                 FontSize="13" BorderBrush="#CCCCCC"
                 PreviewKeyDown="TBox_SN_PreviewKeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
            <Button Content="取消" Width="80" Height="32" Margin="0,0,10,0"
                    Click="Button_Click_Cancel"/>
            <Button Content="确定" Width="80" Height="32"
                    Click="Button_Click_Confirm"/>
        </StackPanel>
    </Grid>
</Window>
